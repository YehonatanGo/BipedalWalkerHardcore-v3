# --------------------------------discritization------------------------------------#
discrete_values = [-1, 0, 1]
discrete_actions = [(x, y, z, w) for x in discrete_values for y in discrete_values for z in discrete_values for w in
                    discrete_values]
# ----------------------------------------------------------------------------------#
